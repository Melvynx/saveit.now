---
title: Delete Bookmark
description: Delete an existing bookmark using the SaveIt.now API
method: DELETE
endpoint: /api/v1/bookmarks/{bookmarkId}
order: 4
category: API
subcategory: Bookmarks
tags: [api, bookmarks, delete]
published: true
examples:
  bash: |
    curl -X DELETE https://saveit.now/api/v1/bookmarks/bm_1234567890 \
      -H "Authorization: Bearer YOUR_API_KEY"
  javascript: |
    const response = await fetch('https://saveit.now/api/v1/bookmarks/bm_1234567890', {
      method: 'DELETE',
      headers: {
        'Authorization': 'Bearer YOUR_API_KEY',
      }
    });

    const data = await response.json();
    console.log(data);
  python: |
    import requests

    url = "https://saveit.now/api/v1/bookmarks/bm_1234567890"
    headers = {
        "Authorization": "Bearer YOUR_API_KEY"
    }

    response = requests.delete(url, headers=headers)
    data = response.json()
    print(data)
results:
  success: |
    {
      "success": true,
      "message": "Bookmark deleted successfully"
    }
  error: |
    {
      "success": false,
      "error": "Bookmark not found"
    }
---

Delete an existing bookmark from your SaveIt.now account.

## URL Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `bookmarkId` | string | Yes | The unique identifier of the bookmark to delete |

## Error Responses

**404 Not Found** - Bookmark not found or doesn't belong to the user

**401 Unauthorized** - Invalid or missing API key

## Notes

- Deleting a bookmark is permanent and cannot be undone
- Associated files (screenshots, metadata) stored in S3 will also be removed
- You can only delete bookmarks that belong to your account
- The bookmark ID can be obtained from the [List Bookmarks API](/docs/api-bookmarks-list)
