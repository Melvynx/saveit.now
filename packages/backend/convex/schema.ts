import { defineSchema } from "convex/server";

// Define your Convex tables here as the migration progresses, e.g.
//
//   bookmarks: defineTable({
//     userId: v.string(),
//     url: v.string(),
//   }).index("by_userId", ["userId"]),
//
// See packages/backend/convex/_generated/ai/guidelines.md for the schema rules.
export default defineSchema({});
